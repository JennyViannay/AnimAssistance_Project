<form method="post" action="{{ path('ask_for_adoption_delete', {'id': ask_for_adoption.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ask_for_adoption.id) }}">
    <button class="btn">Delete</button>
</form>
